# Supabase Configuration
# Copy this file to .env.local and fill in your values from Project Settings > API

# Your Supabase project URL (needed for both client and server)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Public (anon) key - safe to expose in browser
NEXT_PUBLIC_SUPABASE_PUBLIC_KEY=your-anon-key-here

# Service role key - NEVER expose in browser! Server-side only.
# This key bypasses RLS and is used for admin operations.
SUPABASE_SECRET_KEY=your-service-role-key-here
